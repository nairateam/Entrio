# Public base URL for the Entrio API (used directly in dev)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Production only: the API origin that /api is proxied to (keeps the auth cookie
# first-party). Set this in the Vercel project env, e.g. https://your-api.example.com
API_PROXY_TARGET=
